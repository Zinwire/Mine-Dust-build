Events.on(PlayEvent, () => {
    // Включаем крупный текст на экране
    Vars.ui.hudfrag.setHudText("[orange]ВНИМАНИЕ: МОД ЗАРЯЖЕН[]");
    
    // Запускаем таймер на 5 секунд (5000 миллисекунд), чтобы убрать текст
    Timer.schedule(() => {
        Vars.ui.hudfrag.setHudText(""); // Стираем текст, передав пустую строку
    }, 5.0); // Время в секундах
});
