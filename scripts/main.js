Events.on(PlayEvent, () => {
    // Вызывает крупную надпись по центру экрана. 
    // Текст сам плавно исчезнет через пару секунд.
    Vars.logic.announcement("[scarlet]🚨 СИСТЕМНАЯ ТРЕВОГА 🚨[]");
});
